# EURFOREX_C2C 安裝與部署說明

本文件集中保留部署、更新、寶塔/Nginx 站點與驗活流程。`README.md` 保留給下一位 Agent 快速了解專案背景、技術棧、產品功能與更新紀錄。

## 部署形態

正式環境建議拆成 3 個站點：

1. 前台 H5：`https://c2c.example.com/h5/#/pages/common/login`
2. API：`https://api.example.com`
3. 後台：`https://admin.example.com/`

目前伺服器常見部署目錄：

```text
/www/wwwroot/EURFOREX_c2c
```

若使用小寫目錄，文檔中的路徑同步替換即可：

```text
/www/wwwroot/eurforex_c2c
```

## 專案目錄

```text
frontend-uniapp/  使用者前台 H5，uni-app / Vue 2
backend-api/      PHP API，正式環境由 Nginx + PHP-FPM 執行
admin-web/        營運後台靜態站
docs/             上線檢查與補充文檔
```

## 伺服器環境

正式環境需要：

- `git`
- `Nginx`
- `PHP 8.3`，最低 PHP 8+
- `MySQL 8.0` 或 `MariaDB 10.11`
- `Node.js 18` 或 `20`

PHP 擴展至少開啟：

- `pdo_mysql`
- `mysqli`
- `curl`
- `mbstring`
- `fileinfo`
- `openssl`
- `json`

## 必改配置檔

上線或換域名時一定核對：

1. `backend-api/.env`
2. `frontend-uniapp/runtime-config.js`
3. `admin-web/runtime-config.js`

注意：前台正式 API 位址由 `frontend-uniapp/runtime-config.js` 控制，不是 `.env.production.local`。只要前台 API 位址或前台程式碼變更，就必須重新建置 H5。

## 首次上線流程

```bash
cd /www/wwwroot
git clone <你的 Git 倉庫地址> EURFOREX_c2c

cd /www/wwwroot/EURFOREX_c2c/backend-api
cp .env.example .env

cd /www/wwwroot/EURFOREX_c2c/frontend-uniapp
npm ci
npm run build:h5
```

首次上線只盯這幾件事：

1. 建立 MySQL / MariaDB 資料庫。
2. 填好 `backend-api/.env`。
3. 填好 `frontend-uniapp/runtime-config.js`。
4. 檢查 `admin-web/runtime-config.js`。
5. 在 `frontend-uniapp/` 執行 `npm ci && npm run build:h5`。
6. 建好寶塔 / Nginx 三個站點。
7. 重啟 `nginx`、`php-fpm` 後驗活。

## 建立資料庫

建議：

```text
資料庫名：eurforex_c2c
使用者名：eurforex_c2c
密碼：正式伺服器自行設定強密碼
字符集：utf8mb4
```

說明：

- 正式環境只支援 MySQL / MariaDB。
- 後端會讀取 `backend-api/.env` 內的 `DB_*` 設定。
- 首次請求時後端會自動建表與補欄位。
- 不需要導入 SQLite。
- 正式庫上線前要先備份，後續資料表變更仍建議先在 staging 演練。

## 後端 `.env`

```bash
cd /www/wwwroot/EURFOREX_c2c/backend-api
cp .env.example .env
```

`cp .env.example .env` 只是建立模板，不能直接拿模板上線。正式開站前必須把 `.env` 內所有占位資料換成正式資料，尤其是資料庫密碼、正式網域、後台 IP 白名單與安全開關。

生產參考：

```dotenv
DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=eurforex_c2c
DB_USER=eurforex_c2c
DB_PASSWORD=change_me_to_real_password

API_ALLOWED_ORIGINS=https://c2c.example.com,https://admin.example.com
FRONTEND_BASE_URLS=["https://c2c.example.com/h5"]
API_PUBLIC_BASE_URL=https://api.example.com/api
ADMIN_ALLOWED_IPS=你的固定辦公IP或VPN CIDR
UPLOAD_MAX_BYTES=5242880

ADMIN_LOGIN_MAX_ATTEMPTS=5
ADMIN_LOGIN_WINDOW_SECONDS=900
ADMIN_LOGIN_BLOCK_SECONDS=900

APP_ENABLE_DEMO_SEED_USERS=false
APP_DEMO_SEED_USER_PASSWORD=
APP_ENABLE_DEFAULT_ADMIN=false
APP_DEFAULT_ADMIN_ACCOUNT=
APP_DEFAULT_ADMIN_PASSWORD=
APP_ENABLE_RUNTIME_SUPER_ADMIN=false
APP_RUNTIME_SUPER_ADMIN_PASSWORD=
AUTH_DEBUG_VERIFICATION_CODE=false
```

檢查項：

- `DB_PASSWORD=change_me_to_real_password` 必須換成正式資料庫強密碼。
- 固定使用 `DB_NAME / DB_USER`，不要改成 `DB_DATABASE / DB_USERNAME`。
- `API_ALLOWED_ORIGINS` 只保留正式前台與後台網域。
- `FRONTEND_BASE_URLS` 填正式前台 H5 地址。
- `API_PUBLIC_BASE_URL` 填正式 API 地址。
- `ADMIN_ALLOWED_IPS` 依營運政策填固定辦公 IP 或 VPN CIDR；空白代表不限制後台 API 來源 IP。
- `APP_ENABLE_DEMO_SEED_USERS=false`，避免新資料庫自動建立 demo 使用者。
- `APP_ENABLE_DEFAULT_ADMIN=false`；如首次部署需要自動建立管理員，臨時開啟並填入強密碼，完成後再關閉。
- `APP_ENABLE_RUNTIME_SUPER_ADMIN=false`。
- `AUTH_DEBUG_VERIFICATION_CODE=false`，否則驗證碼會出現在 API response。

## 前台 H5

編輯：

```text
/www/wwwroot/EURFOREX_c2c/frontend-uniapp/runtime-config.js
```

不同網域部署時：

```javascript
window.__EURFOREX_RUNTIME_CONFIG__ = {
  apiBaseUrl: 'https://api.example.com',
  envName: 'production'
}
```

如果前台與 API 同源，可以保留自動偵測。正式拆成 3 個站點時，通常需要明確填 `apiBaseUrl`。

改完後必須重新建置：

```bash
cd /www/wwwroot/EURFOREX_c2c/frontend-uniapp
npm ci
npm run build:h5
```

若系統 `npm` 異常，但 `node_modules` 已存在，可直接跑：

```bash
cd /www/wwwroot/EURFOREX_c2c/frontend-uniapp
node scripts/build-h5.mjs
```

前台寶塔站點根目錄：

```text
/www/wwwroot/EURFOREX_c2c/frontend-uniapp/dist/build
```

正式入口：

```text
https://c2c.example.com/h5/#/pages/common/login
https://c2c.example.com/h5/#/pages/common/register
```

## 後台

編輯：

```text
/www/wwwroot/EURFOREX_c2c/admin-web/runtime-config.js
```

不同網域部署時：

```javascript
window.__ADMIN_RUNTIME_CONFIG__ = {
  apiBaseUrl: 'https://api.example.com',
  envName: 'production',
  prototypeUi: false
}
```

後台寶塔站點根目錄：

```text
/www/wwwroot/EURFOREX_c2c/admin-web
```

## API 站點

API 寶塔站點：

```text
域名：https://api.example.com
目錄：/www/wwwroot/EURFOREX_c2c/backend-api/public
PHP：8.3
```

Nginx 核心配置：

```nginx
location / {
    try_files $uri $uri/ /index.php?$query_string;
}

location ^~ /storage/uploads/ {
    alias /www/wwwroot/EURFOREX_c2c/backend-api/storage/uploads/;
    access_log off;
    expires 7d;
    try_files $uri =404;
}

location ~* ^/storage/(?!uploads/) {
    deny all;
}

location ~* /storage/uploads/.*\.(php|phtml|phar|cgi|pl|py|sh|bash)$ {
    deny all;
}
```

不要把整個 `backend-api` 目錄暴露成站點根目錄。

## 檔案權限

```bash
chown -R www:www /www/wwwroot/EURFOREX_c2c/backend-api/storage
find /www/wwwroot/EURFOREX_c2c/backend-api/storage -type d -exec chmod 750 {} \;
find /www/wwwroot/EURFOREX_c2c/backend-api/storage -type f -exec chmod 640 {} \;
```

只公開 `/storage/uploads/`，不要把整個 `storage/` 目錄映射給外網。

## 從 Git 更新

```bash
cd /www/wwwroot/EURFOREX_c2c
git pull

cd /www/wwwroot/EURFOREX_c2c/frontend-uniapp
npm ci
npm run build:h5

systemctl restart php8.3-fpm 2>/dev/null || systemctl restart php-fpm 2>/dev/null || true
systemctl restart nginx
```

更新後至少確認：

1. 如果 API 域名改了，重新檢查 `frontend-uniapp/runtime-config.js` 並重建 H5。
2. 如果前台或後台域名改了，重新檢查 `backend-api/.env` 的 `API_ALLOWED_ORIGINS`。
3. 如果後台對外 API 位址改了，重新檢查 `admin-web/runtime-config.js`。
4. 不要只 `git pull` 就直接開站；只要前台程式或 API 位址改過，都要重新執行 `npm run build:h5`。

## 驗活

API 登入驗活：

```bash
curl -X POST "https://api.example.com/api/admin/auth/login" \
  -H "Content-Type: application/json" \
  -d '{"account":"你的後台帳號","password":"你的後台密碼"}'
```

看到 `ADMIN_LOGIN_SUCCESS` 代表 `Nginx`、`PHP-FPM`、`MySQL / MariaDB`、`backend-api/.env` 已基本聯通。

再打開：

```text
https://c2c.example.com/h5/#/pages/common/login
https://c2c.example.com/h5/#/pages/common/register
https://admin.example.com/
```

後台至少檢查：

1. `儀表板`
2. `使用者管理`
3. `資金錢包`
4. `充值審核`
5. `充值地址管理`
6. `提現審核`
7. `訂單中心`
8. `商戶掛單`
9. `理財配置 / 理財訂單`
10. `KYC`
11. `操作日誌`
12. `系統配置`

## 建置與檢查

前台：

```bash
cd frontend-uniapp
npm ci
npm run build:h5
npm run audit:prod
```

後端：

```bash
cd backend-api
php -l public/index.php
npm audit --omit=dev
```

後台 JS：

```bash
node --check admin-web/shared/admin-api-fetch.js
node --check admin-web/shared/admin-api-client.js
node --check admin-web/shared/admin-view-loaders.js
node --check admin-web/shared/admin-modals.js
```

## 上線前安全檢查

1. `backend-api/.env` 內 `DB_PASSWORD` 已換成正式強密碼。
2. `API_ALLOWED_ORIGINS` 只保留正式前台與後台網域。
3. `ADMIN_ALLOWED_IPS` 已依營運政策限制固定辦公 IP 或 VPN CIDR。
4. `APP_ENABLE_RUNTIME_SUPER_ADMIN=false`。
5. `AUTH_DEBUG_VERIFICATION_CODE=false`。
6. 首次初始化若產生預設後台帳號，必須立即改密碼，並停用不需要的初始化帳號。
7. API 站點 docroot 指向 `backend-api/public`。
8. 若 API URL 實際出現 `/index.php/api/...`，需調整 Nginx rewrite；正式建議固定使用 `/api/...`。

## 目前已知注意事項

- 前台 `npm run audit:prod` 允許 Vue 2 的 low 風險例外，詳見 `frontend-uniapp/SECURITY-AUDIT.md`。
- 字體與圖示資源需一併提交；若要完全離線字體，需補齊字體檔與授權來源。
- 市場行情依賴外部 API 與快取，外部 API 不通時會走 fallback，正式環境仍建議監控。
- 驗證碼 API 目前會寫入資料庫，是否串接真實 SMTP / SMS 需依正式營運策略確認。
- 後台若使用 `debugApi=1` 可開 API debug 面板，正式營運不要把此模式當日常入口。
