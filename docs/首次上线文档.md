# EURNYSE_C2C 首次上线文档

这份文档用于首次把 `EURNYSE_C2C` 部署到正式服务器时核对配置、记录操作与验收结果。  
本项目与 `New_C2C` 不完全相同：前台 API 配置使用 `frontend-uniapp/runtime-config.js`，不是 `.env.production.local`；目前也没有 `release:fingerprint / release:verify` 脚本。

## 1. 基础信息

- 发版类型：首次上线
- 环境：生产
- 项目目录：`/www/wwwroot/eurnyse_c2c`
- Git 仓库：`<填写 Git 仓库地址>`
- 分支：`main`
- 提交哈希（上线前）：`<填写 commit hash>`
- 提交哈希（上线后）：`<填写 commit hash>`
- 操作人：`<填写操作人>`
- 发版时间（开始）：`<YYYY-MM-DD HH:mm:ss +0800>`
- 发版时间（结束）：`<YYYY-MM-DD HH:mm:ss +0800>`
- 服务器 IP：`<填写服务器 IP>`

## 2. 推荐正式站点

建议拆成 3 个站点：

1. 前台 H5：`https://c2c.example.com/h5/#/pages/common/login`
2. API：`https://api.example.com`
3. 后台：`https://admin.example.com/`

正式部署路径：

- 前台站点根目录：`/www/wwwroot/eurnyse_c2c/frontend-uniapp/dist/build`
- 前台实际 H5 路径：`/h5/`
- API 站点根目录：`/www/wwwroot/eurnyse_c2c/backend-api/public`
- 后台站点根目录：`/www/wwwroot/eurnyse_c2c/admin-web`

## 3. 服务器环境检查

- [ ] 已安装 `git`
- [ ] 已安装 `Nginx`
- [ ] 已安装 `PHP 8.3`，最低需 PHP 8+
- [ ] 已安装 `MySQL 8.0` 或 `MariaDB 10.11`
- [ ] 已安装 `Node.js 18` 或 `20`
- [ ] PHP 已开启 `pdo_mysql`
- [ ] PHP 已开启 `mysqli`
- [ ] PHP 已开启 `curl`
- [ ] PHP 已开启 `mbstring`
- [ ] PHP 已开启 `fileinfo`
- [ ] PHP 已开启 `openssl`
- [ ] PHP 已开启 `json`

## 4. 必改 3 个配置文件

上线前必须核对：

1. `backend-api/.env`
2. `frontend-uniapp/runtime-config.js`
3. `admin-web/runtime-config.js`

重要：`cp .env.example .env` 只是建立模板，不能直接拿模板上线。正式开站前必须把 `.env` 内所有占位资料换成正式资料。

## 5. 后端 `.env` 生产配置

```bash
cd /www/wwwroot/eurnyse_c2c/backend-api
cp .env.example .env
```

生产参考：

```dotenv
DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=eurnyse_c2c
DB_USER=eurnyse_c2c
DB_PASSWORD=change_me_to_real_password

API_ALLOWED_ORIGINS=https://c2c.example.com,https://admin.example.com
FRONTEND_BASE_URLS=["https://c2c.example.com/h5"]
API_PUBLIC_BASE_URL=https://api.example.com/api
ADMIN_ALLOWED_IPS=你的固定办公IP或VPN CIDR
UPLOAD_MAX_BYTES=5242880

ADMIN_LOGIN_MAX_ATTEMPTS=5
ADMIN_LOGIN_WINDOW_SECONDS=900
ADMIN_LOGIN_BLOCK_SECONDS=900

APP_ENABLE_DEMO_SEED_USERS=false
APP_DEMO_SEED_USER_PASSWORD=
APP_ENABLE_DEFAULT_ADMIN=false
APP_DEFAULT_ADMIN_ACCOUNT=
APP_DEFAULT_ADMIN_PASSWORD=
APP_ENABLE_RUNTIME_SUPER_ADMIN=false
APP_RUNTIME_SUPER_ADMIN_PASSWORD=
AUTH_DEBUG_VERIFICATION_CODE=false
```

检查项：

- [ ] `DB_PASSWORD=change_me_to_real_password` 已换成正式数据库强密码
- [ ] `API_ALLOWED_ORIGINS` 只保留正式前台与后台域名
- [ ] `FRONTEND_BASE_URLS` 已填写正式前台 H5 地址
- [ ] `API_PUBLIC_BASE_URL` 已填写正式 API 地址
- [ ] `ADMIN_ALLOWED_IPS` 已填写正式办公 IP 或 VPN CIDR
- [ ] `APP_ENABLE_DEMO_SEED_USERS=false`
- [ ] `APP_ENABLE_DEFAULT_ADMIN=false`；如需首次自动建立管理员，已临时开启并填入强密码，完成后再关闭
- [ ] `APP_ENABLE_RUNTIME_SUPER_ADMIN=false`
- [ ] `AUTH_DEBUG_VERIFICATION_CODE=false`

## 6. 前台 API 地址

编辑 `frontend-uniapp/runtime-config.js`：

```javascript
window.__EURNYSE_RUNTIME_CONFIG__ = {
  apiBaseUrl: 'https://api.example.com',
  envName: 'production'
}
```

检查项：

- [ ] `apiBaseUrl` 已指向正式 API 域名
- [ ] 没有写死 `http://127.0.0.1:8000`
- [ ] 改完后已重新执行 `npm run build:h5`

## 7. 后台 API 地址

编辑 `admin-web/runtime-config.js`：

```javascript
window.__ADMIN_RUNTIME_CONFIG__ = {
  apiBaseUrl: 'https://api.example.com',
  envName: 'production',
  prototypeUi: false
}
```

## 8. 首次构建与部署

```bash
cd /www/wwwroot
git clone <你的 Git 仓库地址> eurnyse_c2c

cd /www/wwwroot/eurnyse_c2c/backend-api
cp .env.example .env

cd /www/wwwroot/eurnyse_c2c/frontend-uniapp
npm ci
npm run build:h5
npm run audit:prod

cd /www/wwwroot/eurnyse_c2c/backend-api
npm audit --omit=dev
php -l public/index.php
```

## 9. Nginx 站点

API 站点：

```text
域名：https://api.example.com
目录：/www/wwwroot/eurnyse_c2c/backend-api/public
PHP：8.3
```

API Nginx 核心配置：

```nginx
location / {
    try_files $uri $uri/ /index.php?$query_string;
}

location ^~ /storage/uploads/ {
    alias /www/wwwroot/eurnyse_c2c/backend-api/storage/uploads/;
    access_log off;
    expires 7d;
    try_files $uri =404;
}

location ~* ^/storage/(?!uploads/) {
    deny all;
}

location ~* /storage/uploads/.*\.(php|phtml|phar|cgi|pl|py|sh|bash)$ {
    deny all;
}
```

前台站点：

```text
域名：https://c2c.example.com
目录：/www/wwwroot/eurnyse_c2c/frontend-uniapp/dist/build
类型：静态站
```

后台站点：

```text
域名：https://admin.example.com
目录：/www/wwwroot/eurnyse_c2c/admin-web
类型：静态站
```

## 10. 文件权限

```bash
chown -R www:www /www/wwwroot/eurnyse_c2c/backend-api/storage
find /www/wwwroot/eurnyse_c2c/backend-api/storage -type d -exec chmod 750 {} \;
find /www/wwwroot/eurnyse_c2c/backend-api/storage -type f -exec chmod 640 {} \;
```

只公开 `/storage/uploads/`，不要把整个 `storage/` 目录映射给公网。

## 11. 最后验活

API 登录验活：

```bash
curl -X POST "https://api.example.com/api/admin/auth/login" \
  -H "Content-Type: application/json" \
  -d '{"account":"你的后台账号","password":"你的后台密码"}'
```

通过标准：

- [ ] 返回 `code=1`
- [ ] 返回 `success_code=ADMIN_LOGIN_SUCCESS`
- [ ] 返回 `token`

页面验活：

- [ ] 前台登录页能打开
- [ ] 前台注册页能打开
- [ ] 后台登录页能打开
- [ ] 后台 `使用者管理` 能打开
- [ ] 后台 `资金钱包` 能打开
- [ ] 后台 `充值地址管理` 能打开
- [ ] 后台 `订单中心` 能打开
- [ ] 后台 `系统配置` 能打开

## 12. 上线前安全检查

- [ ] 正式 `.env` 不含本机 DB 密码
- [ ] 正式 `.env` 不含测试 API 域名
- [ ] `AUTH_DEBUG_VERIFICATION_CODE=false`
- [ ] `APP_ENABLE_RUNTIME_SUPER_ADMIN=false`
- [ ] 后台默认账号已改密或停用
- [ ] `ADMIN_ALLOWED_IPS` 已填写正式办公 IP 或 VPN CIDR
- [ ] API 站点 docroot 没有暴露整个 `backend-api`
- [ ] 上传目录禁止执行 PHP / Shell 脚本
- [ ] 不用 `php -S`、`npm run serve:dist` 作为正式服务

## 13. 已知提醒

- 字体与图示资源需一并提交；目前仓库包含 `PNG/` 图示素材与 `frontend-uniapp/static/previews/shared/fonts/google-cdn-mirror/FONT-SOURCE.txt` 来源说明，但尚未包含 `.woff/.woff2/.ttf/.otf` 字体文件。如需完全离线字体，需先补齐字体文件与授权来源再提交。
- 行情与新闻依赖外部 API 与缓存，正式环境需观察外部 API 超时情况。
- 验证码 API 当前会写入数据库；是否串接真实 SMTP / SMS 需按正式运营策略确认。

## 14. 回滚预案

```bash
cd /www/wwwroot/eurnyse_c2c
git log --oneline -5
git checkout <上一版稳定 commit>

cd frontend-uniapp
npm ci
npm run build:h5

systemctl restart php8.3-fpm 2>/dev/null || systemctl restart php-fpm 2>/dev/null || true
systemctl restart nginx
```

## 15. 发版结论

- [ ] 发版成功，可持续运行
- [ ] 部分成功，需补操作
- [ ] 发版失败，已回滚

最终结论说明：

```text
<填写最终结论>
```
